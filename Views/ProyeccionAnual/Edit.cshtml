@model ProyectoCore.Models.ProyeccionAnual

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>ProyeccionAnual</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="IdProyeccionAnual" />
            <div class="form-group">
                <label asp-for="IdExportacion" class="control-label"></label>
                <select asp-for="IdExportacion" class="form-control" asp-items="ViewBag.IdExportacion"></select>
                <span asp-validation-for="IdExportacion" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="IdTrabajador" class="control-label"></label>
                <select asp-for="IdTrabajador" class="form-control" asp-items="ViewBag.IdTrabajador"></select>
                <span asp-validation-for="IdTrabajador" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Cantidad" class="control-label"></label>
                <input asp-for="Cantidad" class="form-control" />
                <span asp-validation-for="Cantidad" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
